import React, { Component } from "react";

class category extends Component {
  render() {
    return (
      <form
        input
        type="hidden"
        value="animal"
        id="categoryInput"
      />
    );
  }
}

export default category;
