import React, { Component } from "react";

class Quote_button extends Component {
  render() {
    return (
      <button onclick={Quote_button}> Refresh Quote </button>
    );
  }
}

export default Quote_button;
